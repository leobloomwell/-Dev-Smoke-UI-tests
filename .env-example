# ============================================
# Bloomwell DEV - Test Environment Variables
# ============================================
# 
# Copy this file to .env and fill in your actual credentials
# DO NOT commit .env to version control
#
# Command: cp .env.example .env

# ============================================
# Basic Authentication Credentials
# ============================================
# Used for HTTP Basic Auth in playwright.config.ts and login.spec.ts
# These credentials are required for accessing the DEV environment
BASIC_AUTH_USERNAME=your_basic_auth_username
BASIC_AUTH_PASSWORD=your_basic_auth_password

# ============================================
# UI Login Credentials
# ============================================
# Used for form-based login in login.spec.ts
# These are the admin portal UI login credentials
UI_LOGIN_EMAIL=your_ui_login_email@example.com
UI_LOGIN_PASSWORD=your_ui_login_password

# ============================================
# Slack Integration (Optional)
# ============================================
# Used for Slack reporter in playwright.config.ts
# Leave empty if Slack reporting is not needed
SLACK_BOT_USER_OAUTH_TOKEN=your_slack_oauth_token
SLACK_CHANNELS=your_slack_channel_id